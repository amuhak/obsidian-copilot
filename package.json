{
	"name": "ghostwriter",
	"version": "1.0.1",
	"description": "Inline ghost-text completion and cursor-anchored AI edits, powered by your own local LLM server.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"install-local": "node scripts/install-local.mjs",
		"deploy": "npm run build && npm run install-local",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "Amulya Jain",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"esbuild": "^0.20.0",
		"obsidian": "latest",
		"tslib": "^2.6.2",
		"typescript": "^5.3.3"
	},
	"dependencies": {
		"@codemirror/state": "^6.0.0",
		"@codemirror/view": "^6.0.0"
	}
}
